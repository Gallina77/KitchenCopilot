services:
  ci_db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kitchencopilot_ci
    ports:
      - "5432:5432"
    volumes:
      - kitchencopilot_ci_data:/var/lib/postgresql/data

volumes:
  kitchencopilot_ci_data:
